ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: example-issuer
  hosts:
    - host: uptime-demo.92.co.ucll.cloud
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: uptime-demo-kuma-tls
      hosts:
        - uptime-demo.92.co.ucll.cloud
